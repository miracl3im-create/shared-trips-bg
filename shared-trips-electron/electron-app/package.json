{
  "name": "shared-trips-bg",
  "version": "1.0.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "start": "cross-env VITE_API_BASE=http://localhost:4777/api electron .",
    "build": "cd renderer && cross-env VITE_API_BASE=http://localhost:4777/api npm run build",
    "postinstall": "electron-builder install-app-deps",
    "dist": "npm run build && cross-env NODE_ENV=production electron-builder -w nsis"
  },

  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "electron-is-dev": "^2.0.0"
  },

  "devDependencies": {
    "electron": "31.7.7",
    "electron-builder": "^24.13.3",
    "cross-env": "^7.0.3",
    "vite": "^5.4.19"
  }

  /* ако имаш "build": { ... } за electron-builder – остави го непроменен */
}
